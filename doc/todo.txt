1) [ ] Фильтры выходных имён
2) [X] Отфильтровать все operator* - имена, содержащие <=>?!~[]&|+-/*%.
3) [ ] Отфильтровать всё по типу: --class, --typedef, --enum, --function, --define, --type(class/typedef/enum), --all (default) - KindOfKind
4) [ ] Опция, как генерить инклуды - с кавычками или угловыми скобками: --quoted-include
5) [X] Преобразовать имена типов в имена файлов
6) [ ] Просканировать файлы на предмет define - со шлангом довольно геморно,
       сделаю ручками - все строки, начинающиеся с #define - берём все валидные символы после #define.
       Не забыть, что допустимы пробелы между '#' и "define".
       Отдельно - если надо избежать попадания макроса в генерируемое моей тулзой - 
       можно не задавать правила для этого, а просто использовать line continuation
       #define \
               MACRO_NAME \
               ...
